<%= user.name %>
<%= attachment_image_tag user, :image, size: '140x100' %>
<%= user.introduction %>
<% if current_user && user.id == current_user.id %>
  <p><%= link_to "プロフィール編集", edit_user_path(user) %></p>
<% end %>
<!--お気に入り一覧-->
<%= link_to "お気に入りなんで事典", "/users/#{user.id}/favorites" %>